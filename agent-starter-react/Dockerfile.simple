# Ultra-simple Dockerfile for Next.js
FROM node:18

WORKDIR /app

# Copy everything
COPY . .

# Install dependencies with npm (more reliable than pnpm in containers)
RUN npm install

# Build
ENV NEXT_TELEMETRY_DISABLED=1
RUN npm run build

# Expose port
EXPOSE 3000

# Start
CMD ["npm", "start"]
